@model IEnumerable<tjadmin.Models.tbl_Lang>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            lang_name_en
        </th>
        <th>
            lang_name_vi
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.lang_name_en)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lang_name_vi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.lang_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.lang_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.lang_Id })
        </td>
    </tr>
}

</table>
