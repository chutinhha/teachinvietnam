@model IEnumerable<tjadmin.Models.tbl_Job_JobLevel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            jrName_en
        </th>
        <th>
            jrName_vi
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.jrName_en)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.jrName_vi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.jrId }) |
            @Html.ActionLink("Details", "Details", new { id=item.jrId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.jrId })
        </td>
    </tr>
}

</table>
