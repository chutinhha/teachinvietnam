@model IEnumerable<tjadmin.Models.tbl_CV_EduLevel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            el_name_en
        </th>
        <th>
            el_name_vi
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.el_name_en)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.el_name_vi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.el_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.el_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.el_id })
        </td>
    </tr>
}

</table>
