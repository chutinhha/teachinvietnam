@model IEnumerable<tjadmin.Models.tbl_cv_YearsOfExp>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            yoe_name_en
        </th>
        <th>
            yoe_name_vn
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.yoe_name_en)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.yoe_name_vn)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.yoe_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.yoe_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.yoe_id })
        </td>
    </tr>
}

</table>
